UPLOAD EVERY FILE IN THIS FOLDER TO THE REPOSITORY ROOT.

The homepage must be named exactly index.html.
Do not upload the ZIP itself. Extract it first, then select every individual file.

Required files include all HTML pages, styles.css, main.js, every image, CNAME, robots.txt, sitemap.xml, site.webmanifest and .nojekyll.
